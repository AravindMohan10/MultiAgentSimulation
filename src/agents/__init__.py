"""
Agent package.

Intended contents:
- A base agent interface with a `step(observation)` method.
- Concrete agent implementations (heuristic, local model-based, API-based LLM agents).
- Optional utilities for environment-mediated communication protocols between agents.

Implementation is left to the main author.
"""

